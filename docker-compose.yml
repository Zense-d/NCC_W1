services:
  api-service:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: fauzan_api_service
    ports:
      - "80:8000"
    env_file:
      - .env
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
